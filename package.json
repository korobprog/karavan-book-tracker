{
  "name": "karavan-book-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "tracker": "npm run start -w tracker",
    "tracker:build": "npm run build -w tracker",
    "tracker:deploy": "npm run deploy -w tracker",
    "admin": "npm run start -w admin",
    "admin:build": "npm run build -w admin",
    "admin:deploy": "npm run deploy -w admin"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "workspaces": [
    "packages/common",
    "packages/admin",
    "packages/tracker"
  ]
}
